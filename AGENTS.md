never view the resulting UI using a broweser. ask the user to check if needed

we want to keep this grug. 1-1 mappings. one source table is equal to one ingestion yaml is equal to one destination table
